// @flow
import { md, lg } from '~/theme/variables'

export const styles = () => ({
  txDataContainer: {
    padding: `${lg} ${md}`,
  },
  rightCol: {
    boxSizing: 'border-box',
    borderLeft: 'solid 1px #c8ced4',
  },
})
