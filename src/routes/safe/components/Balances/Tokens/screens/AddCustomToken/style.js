// @flow
import { lg, bolderFont } from '~/theme/variables'

export const styles = () => ({
  title: {
    padding: `${lg} 20px 20px`,
    fontWeight: bolderFont,
    fontSize: '16px',
  },
  formContainer: {
    padding: '0 20px',
  },
  addressInput: {
    marginBottom: '15px',
  },
})
