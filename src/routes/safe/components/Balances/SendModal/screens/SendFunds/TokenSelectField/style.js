// @flow
import { sm } from '~/theme/variables'

export const selectedTokenStyles = () => ({
  container: {
    minHeight: '55px',
    padding: 0,
    width: '100%',
  },
  tokenData: {
    padding: 0,
  },
  tokenImage: {
    marginRight: sm,
  },
})

export const selectStyles = () => ({
  selectMenu: {
    paddingRight: 0,
  },
})
