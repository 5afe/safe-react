// @flow
import { lg } from '~/theme/variables'

export const styles = () => ({
  formContainer: {
    padding: lg,
    minHeight: '369px',
  },
  root: {
    display: 'flex',
    maxWidth: '460px',
  },
})
