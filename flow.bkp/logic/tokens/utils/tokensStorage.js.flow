// @flow
import { List, Map } from 'immutable';
import { type Token, type TokenProps } from 'src/logic/tokens/store/model/token';
import { loadFromStorage, saveToStorage } from 'src/utils/storage';
export const ACTIVE_TOKENS_KEY = 'ACTIVE_TOKENS';
export const CUSTOM_TOKENS_KEY = 'CUSTOM_TOKENS'; // Tokens which are active at least in one of used Safes in the app should be saved to localstorage
// to avoid iterating a large amount of data of tokens from the backend
// Custom tokens should be saved too unless they're deleted (marking them as inactive doesn't count)

declare export var saveActiveTokens: (tokens: Map<string, Token>) => any;
declare export var getActiveTokens: () => Promise<Object<string, TokenProps>>;
declare export var getCustomTokens: () => Promise<List<TokenProps>>;
declare export var removeTokenFromStorage: (safeAddress: string, token: Token) => any;
declare export var removeFromActiveTokens: (safeAddress: string, token: Token) => any;