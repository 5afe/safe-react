// @flow
import type { Dispatch as ReduxDispatch } from 'redux';
import { createAction } from 'redux-actions';
import { type Token } from 'src/logic/tokens/store/model/token';
import { removeFromActiveTokens, removeTokenFromStorage } from 'src/logic/tokens/utils/tokensStorage';
import { type GlobalState } from 'src/store/index';
export const REMOVE_TOKEN = 'REMOVE_TOKEN';
type RemoveTokenProps = {
  safeAddress: string,
  token: Token,
};
export const removeToken = createAction<string, *, *>(REMOVE_TOKEN, (safeAddress: string, token: Token): RemoveTokenProps => ({
  safeAddress,
  token
}));
declare var deleteToken: (safeAddress: string, token: Token) => any;
export default deleteToken;