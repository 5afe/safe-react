// @flow
import { List, Map } from 'immutable';
import { type Selector, createSelector } from 'reselect';
import { type Token } from 'src/logic/tokens/store/model/token';
import { TOKEN_REDUCER_ID } from 'src/logic/tokens/store/reducer/tokens';
import { type RouterProps } from 'src/routes/safe/store/selectors';
import { type GlobalState } from 'src/store';
declare export var tokensSelector: (state: GlobalState) => any;
export const tokenListSelector: Selector<GlobalState, Map<string, Token>, List<Token>> = createSelector(tokensSelector, (tokens: Map<string, Token>) => tokens.toList());
export const orderedTokenListSelector: Selector<GlobalState, RouterProps, List<Token>> = createSelector(tokenListSelector, (tokens: List<Token>) => tokens.sortBy((token: Token) => token.get('symbol')));